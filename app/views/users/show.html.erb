<h1><%= @post.user.name  %>さんの投稿</h1>
<p>
<% if user_signed_in? %>
  <% unless current_user.id == @post.user_id %>
  <%= render 'users/follow_form' %>
<% end %>
</p>
<section class="stats">
  <%= render 'users/stats_second' %>
</section>
    <ul class='item-lists'>
      <li class='list'>
        <div class='item-img-content'>
          <%# <% @post.each do |post| %> %>
            <%# <%= link_to user_path(post.id) do %> %>
              <% @post.images.each do |image| %>
                <%= image_tag(image, class: "item-img") %>
              <% end %>
                <iframe src="https://www.youtube.com/embed/<%= @post.youtube_url%>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            <%# <% end %> %>
          <%# <% end %> %>
        </div>
      </li>
    </ul>
<%end%>
  <% if user_signed_in? %>
    <% if current_user.id == @post.user.id %>
      <%= link_to "投稿の編集", edit_myroom_path, method: :get, class: "item-red-btn" %>
      <p class="or-text">or</p>
      <%= link_to "削除", myroom_path, method: :delete, class:"item-destroy" %>
    <% end %>
  <% end %>
<p><%= link_to "戻る", root_path %></p>
<% breadcrumb :index_show %>
  <li><%= link_to @post.user.name+"さんの全投稿を見る", myroom_post_path(@post.user_id) %></li>