<h1>フォロ一フォーム</h1>
 <% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.following?(@post) %>
    <%= render 'users/unfollow' %>
  <% else %>
    <%= render 'users/follow' %>
  <% end %>
  </div>
<% end %>  
